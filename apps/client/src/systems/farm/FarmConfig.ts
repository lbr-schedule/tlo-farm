// FarmConfig.ts — 農地初始設定
// M003.1: 集中農地尺寸常數，未來支援解鎖時再改為動態讀取

export const INITIAL_FARM_COLS = 3;
export const INITIAL_FARM_ROWS = 2;
export const INITIAL_FARM_PLOT_COUNT = INITIAL_FARM_COLS * INITIAL_FARM_ROWS; // 6
